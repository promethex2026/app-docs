---
title: API Overview
description: "Base URLs, authentication, response format, error codes, pagination, and rate limits for the PrometheX API"
---

## Base URL

| Environment | Base URL |
|-------------|----------|
| **Staging** | `https://api-staging.promethex.market` |
| **Production** | `https://api.promethex.market` |

All endpoints use the `/api/v1/` prefix.

```
https://api-staging.promethex.market/api/v1/{endpoint}
```

## Authentication

PrometheX uses **Privy JWT** tokens. Include the token in the `Authorization` header for authenticated endpoints:

```bash
Authorization: Bearer eyJhbGciOiJFUzI1NiIs...
```

Read-only endpoints (market listings, details, prices) are **public**. Any state-changing operation (trading, profile updates) requires authentication. See the [Authentication](/api-reference/authentication) page for the full flow.

## Response Format

Every response follows this envelope:

```json
{
  "code": 0,
  "reason": "",
  "msg": "success",
  "data": { ... }
}
```

<ResponseField name="code" type="number">
`0` for success, non-zero for errors. See [Error Codes](/api-reference/errors).
</ResponseField>

<ResponseField name="reason" type="string">
Machine-readable error key (e.g., `PARAM`, `NOT_FOUND`). Empty on success.
</ResponseField>

<ResponseField name="msg" type="string">
Human-readable message.
</ResponseField>

<ResponseField name="data" type="object">
Response payload. Varies by endpoint. `null` on error.
</ResponseField>

### Success Example

```json
{
  "code": 0,
  "reason": "",
  "msg": "success",
  "data": {
    "totalCount": 42,
    "markets": [...]
  }
}
```

### Error Example

```json
{
  "code": 110101,
  "reason": "PARAM",
  "msg": "marketAddress is required",
  "data": null
}
```

## Error Code Ranges

| Range | Category | Description |
|-------|----------|-------------|
| `110100–110199` | Client | Bad request, missing params, not found |
| `110200–110299` | Server | Internal errors, database, cache |
| `110300–110399` | Third-party | External service failures |

See the full [Error Reference](/api-reference/errors) for all codes and remediation steps.

## Pagination

List endpoints accept `page` (1-indexed) and `pageSize` query parameters:

<CodeGroup>

```bash cURL
curl "https://api-staging.promethex.market/api/v1/market/get-markets?page=1&pageSize=20"
```

```javascript JavaScript
const res = await fetch(
  'https://api-staging.promethex.market/api/v1/market/get-markets?page=1&pageSize=20'
);
const { data } = await res.json();
console.log(data.totalCount); // total items across all pages
```

</CodeGroup>

<ParamField query="page" type="number" required>
  Page number, starting from 1.
</ParamField>

<ParamField query="pageSize" type="number" required>
  Items per page. Maximum `50`.
</ParamField>

Paginated responses include a `totalCount` field for calculating total pages.

## Base Token Filter

Most market endpoints accept an optional `baseTokenAddress` query parameter to filter by the collateral token (e.g., USDC vs Points):

```
GET /api/v1/market/get-markets?page=1&pageSize=10&baseTokenAddress=0x...
```

## HTTP Methods

| Method | Usage |
|--------|-------|
| `GET` | Read operations — markets, users, positions, prices |
| `POST` | Write operations — trading, profile updates, SSE connections |

## CORS

All origins are allowed. Supported methods: `GET`, `POST`, `OPTIONS`.

## Rate Limiting

The API uses **BBR-based adaptive rate limiting**. If you receive error code `110103` (`PLEASE_WAIT`), back off and retry with exponential delay. See [Rate Limits](/api-reference/rate-limits) for details.

## Endpoint Map

| Section | Endpoints | Auth | Description |
|---------|:---------:|:----:|-------------|
| [Markets](/api-reference/markets/list-markets) | 8+ | Mixed | List, search, and query market data |
| [Trading](/api-reference/trading/deposit) | 4 | Yes | Buy, sell, claim, and view positions |
| [Liquidity](/api-reference/liquidity/add-liquidity) | 2 | Yes | Add and remove liquidity |
| [Resolution](/api-reference/resolution/settle-market) | 2 | Yes | Settle markets, check status |
| [Real-time](/api-reference/websocket) | 3 | Yes | SSE event streaming |
| [Users](/api-reference/users/login) | 5 | Mostly | Login, profile, notifications |
| [Community](/api-reference/community/posts) | 4 | Mixed | Posts, comments |

## Quick Links

<CardGroup cols={3}>
  <Card title="Quickstart" icon="rocket" href="/api-reference/quickstart">
    Get trading in 5 steps
  </Card>
  <Card title="Authentication" icon="lock" href="/api-reference/authentication">
    Privy JWT flow
  </Card>
  <Card title="SDKs" icon="code" href="/api-reference/sdks">
    Client libraries
  </Card>
</CardGroup>
